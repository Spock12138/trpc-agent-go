# tRPC-Agent 开源实战选题上下文摘要

> 用途：作为后续新对话窗口的上下文，帮助继续学习、设计并最终完成本次 tRPC-Agent 开源实战选题。
>
> 当前目标：以 Go 为实现语言，尽量控制范围，形成能够提交给 `trpc-group/trpc-agent-go` 上游的、可测试且可审查的 PR 成果。

## 一、题目来源与总体判断

- 官方项目页：<https://opensource.tencent.com/summer-of-code/project/111/practice>
- 本地题目原文：`开源实战.md`
- 课题名称：多租户与节点部署

这不是一个单纯的“接入聊天机器人”课题，而是围绕 tRPC-Agent 构建或补齐一套生产级 Agent 接入与运行能力。题目覆盖：

1. 多租户配置与隔离。
2. 多节点、无状态 Worker 和 Session 路由。
3. Session、Memory、Summary、Artifact、Knowledge、Audit Log 等多种数据的存储抽象。
4. 至少两种 IM 软件接入。
5. 消息验签、租户识别、消息去重和身份映射。
6. 工具权限、预算、危险操作确认、日志脱敏和密钥管理。
7. OpenTelemetry、指标、审计日志和全链路 Trace。
8. 故障恢复、灰度、回滚、容量评估和部署方案。

完整业务链路可以概括为：

```text
用户在 IM 中发送消息
  → IM 平台向服务端发送回调
  → Channel Adapter 验签并转换消息
  → 根据 IM 账号绑定识别 tenant_id
  → 根据平台 message_id 做消息去重
  → 生成 user_id 和 session_id
  → Gateway 选择对应租户的 Agent、模型、工具及数据后端
  → Worker 执行 Runner、模型和工具
  → 保存 Session / Memory / Summary / Audit
  → 将 Agent Event 转换成 IM 回复
  → 记录指标和全链路 Trace
```

## 二、基础概念

### 1. IM 和 IM 消息

IM 是 Instant Messaging，即即时通讯软件，例如：

- 企业微信、微信客服、微信公众号；
- Telegram；
- 飞书、钉钉；
- Slack、Discord、WhatsApp。

一条 IM 消息不只有文字，通常还包括：

- 平台和机器人账号；
- 发送者 ID；
- 群聊、单聊和会话标识；
- 平台消息 ID；
- 时间戳；
- 图片、文件、音频等附件；
- 用于验证来源的签名。

### 2. 为什么要验签

验签用于确认 HTTP 回调确实来自企业微信、Telegram 等合法平台，并且内容没有被篡改。若不验签，攻击者可以伪造消息，消耗模型费用或诱导系统执行危险工具。

### 3. 为什么要识别租户

一个部署可能同时服务多家公司或团队。每个租户拥有不同的：

- Agent 和 Prompt；
- 模型与 API Key；
- 工具权限；
- IM 机器人账号；
- Session、Memory 和知识库；
- 数据后端和审计策略。

`tenant_id` 应由已经通过验签的 IM 账号、企业 ID、Bot ID 或 Webhook 绑定关系反查得到，不能直接信任外部请求自行声明的 `tenant_id`。

### 4. 为什么要消息去重

IM 平台在网络超时或未及时收到确认时，可能重复投递同一条消息。如果不去重，会产生：

- 重复模型调用和费用；
- 重复回复；
- 重复创建工单或发送邮件；
- 危险工具或付款、退款操作执行两次。

典型去重键为：

```text
tenant_id + channel_binding_id + platform_message_id
```

### 5. Session 是什么

Session 表示一段连续对话，使 Agent 能理解“什么时候到？”仍然是在询问上一轮的订单。Session ID 需要同时考虑租户、渠道、群聊或单聊，例如：

```text
tenant-a:wecom:dm:user-9527
tenant-a:telegram:group:group-1001
```

### 6. 什么是多节点部署

这里的节点指一个运行中的服务进程、容器或 Kubernetes Pod，不是 GraphAgent 的流程节点。

多节点部署意味着同时运行多个 Gateway 或 Worker，以获得：

- 水平扩容；
- 节点故障后的高可用；
- 灰度发布；
- 更高的并发处理能力。

如果同一 Session 的不同消息落到不同 Worker，Worker 必须从共享 Redis、SQL 或其他后端读取状态。因此倾向采用无状态 Worker，而不是依赖某台机器的本地内存。

无状态 Worker 不代表同一 Session 可以无序并发执行。同一个 Session 仍需要通过分区队列、分布式锁或版本号/CAS 保证顺序和一致性。

## 三、导师已经确认的内容

导师回复的核心结论如下：

1. 官方页面原先写的 `tRPC-Agent-Python` 已改为 `tRPC-Agent`，允许使用 Go 语言作为实战目标。
2. 最终交付物可根据题目背景灵活选择。
3. 仓库已有 Telegram 只有在设计和实现中真正接入整体方案，才能算“两种 IM”中的一种；仅在文档中提到它不算。
4. 多后端迁移的交付形式可以根据题目背景灵活发挥。

需要注意：导师对第 4 点使用的是“灵活发挥”，没有逐字确认“只做接口和测试一定足够”。正式编码前，最好再提交一次明确的范围说明，获得导师认可。

## 四、当前已经做出的方向选择

### 已确定或倾向的选择

1. 使用 Go 和 `trpc-agent-go` 仓库完成选题。
2. 最终主要成果希望是上游 PR，而不是只提交外部设计文档。
3. 将仓库已有 Telegram 正式纳入双 IM 方案。
4. 新增一种真实 IM Channel Adapter。
5. 多后端迁移主要交付设计、接口和验证测试，不实现完整的生产级在线迁移系统。
6. 通过限制实现范围降低难度，但保持完整业务链路。

### 范围控制原则

应当收缩“实现宽度”，不能切断关键闭环。

合理的收缩：

- 只新增一种 IM，而不是实现所有微信产品；
- 只验证两个租户；
- 只选择两种典型数据后端做迁移或一致性测试；
- 多节点通过共享后端、消息幂等和测试证明，不搭建完整生产集群；
- 迁移做到接口、流程、校验和测试，不做完整双写平台；
- Admin API、预算、Kubernetes 等只完成与主链路有关的最小内容或设计说明。

不合理的收缩：

- 只增加一个 IM SDK 调用，没有租户绑定；
- 仅在文档中提到 Telegram，但代码和测试没有复用它；
- 没有消息去重、Session 隔离或可靠性验证；
- 创建大量抽象接口，但没有任何真实实现或测试使用。

## 五、当前建议的选题定位

建议暂定为：

> 基于 tRPC-Agent-Go OpenClaw Gateway 的多租户双 IM 接入与可靠消息处理：复用 Telegram，新增一种 IM，实现可信租户绑定与消息幂等，并提供多后端迁移接口和一致性验证方案。

这个定位的核心不是做一个完整 SaaS 平台，而是完成一条小而完整、可以验证的纵向链路。

## 六、建议的最小完整成果

### 1. 双 IM

- 复用现有 Telegram；
- 新增一种真实 IM；
- 两种 IM 共同转换为统一内部消息模型；
- 两种 IM 共同经过租户识别、Session 生成、Runner 和回复链路。

新 IM 选择建议：

- 能获得企业微信测试账号和回调环境时，优先企业微信；
- 如果企业微信账号、域名或审核条件会拖慢进度，可选择飞书、Slack 等容易测试的 IM；
- 微信公众号、微信客服的账号和协议条件通常更复杂，不建议作为第一选择。

### 2. 可信租户绑定

增加或明确 `ChannelBinding` 概念：

```text
channel + external_account_id / bot_id / corp_id
  → channel_binding
  → tenant_id + agent_app_id + credential_ref
```

租户身份必须由服务端绑定关系解析，不能依赖用户提供未经信任的 `tenant_id`。

### 3. 消息幂等

建议引入类似 `InboxStore` 的最小能力：

- 使用平台消息 ID 认领消息；
- 重复消息不再次执行 Agent；
- 支持处理中、成功、失败等状态；
- 多节点条件下需要共享实现，InMemory 仅用于单元测试；
- 可使用 Redis 或 SQL 实现最小分布式验证。

如果处理范围允许，可进一步说明 Reply Outbox，但不一定要完成完整生产实现。

### 4. 多租户运行配置

复用仓库已有 Runtime Profile：

- 按 tenant/channel/user/session 选择 Profile；
- 每个 Profile 选择不同 AppName、Agent、模型、工具和知识库；
- 选择失败时 fail closed；
- 使用 AppName 或独立 namespace 隔离 Session 和 Memory。

### 5. 无状态 Worker 和多节点说明

- 使用 Redis、SQL 等共享 Session 后端；
- Worker 不依赖本地会话状态；
- 同一 Session 通过队列分区、锁或版本控制保证顺序；
- 节点故障后请求可以由其他 Worker 接管；
- 不要求实际部署大型 Kubernetes 集群，可以用集成测试和最小 Compose 方案验证。

### 6. 多后端迁移

建议限制为：

- 明确源后端、目标后端和迁移能力接口；
- 全量导出、导入和校验流程；
- 重复执行的幂等性；
- 失败重试或断点续传语义；
- 迁移前后 Session/Event/State/Summary 的一致性比较；
- 至少两个现有后端参与验证；
- 文档说明真正在线迁移所需的双写、灰度切流和回滚，但不全部实现。

避免直接修改所有 `session.Service` 实现。优先考虑新增可选能力接口、包装器或独立测试框架，降低对上游公共 API 的破坏。

### 7. 观测与审计

最小要求：

- Trace 覆盖 IM callback、租户解析、去重、Runner、Tool、Session/Memory 和 IM reply；
- 指标至少包含请求数、重复消息数、执行延迟、错误率、IM 投递状态和 Token 使用；
- 审计字段包含 tenant、channel、user、session、agent、tool、decision、latency、error、cost 和 trace ID；
- API Key、IM Token、数据库密码和敏感消息内容不能进入日志或 Trace。

## 七、仓库已经提供的基础

当前 Go 仓库不是从零开始，已有能力包括：

1. `openclaw/runtimeprofile`
   - tenant/channel/user/session selector；
   - fail-closed Profile 选择；
   - Agent、模型、Prompt、工具、知识、凭据和隔离策略；
   - Profile 可转换为逐请求 `RunOptions`。

2. `agent` 与 `runner`
   - 逐请求 AppName；
   - 动态 Agent 或 AgentFactory；
   - ToolFilter、ToolExecutionFilter、ToolPermissionPolicy；
   - Plugin、Callback 和 Guardrail；
   - Runner 事件流和 Session 持久化。

3. `openclaw`
   - HTTP Gateway；
   - 统一 MessageRequest 和 StreamEvent；
   - Telegram Channel；
   - Channel 和 Backend Factory Registry；
   - 默认单聊、群聊 Session ID 规则。

4. 数据接口和实现
   - Session：InMemory、Redis、SQLite、MySQL、PostgreSQL、MongoDB、ClickHouse、pgvector 等；
   - Memory：InMemory、Redis、SQLite、MySQL、PostgreSQL、pgvector、Mem0、TencentDB 等；
   - Artifact：InMemory、S3、COS；
   - Knowledge：InMemory、pgvector、Qdrant、Milvus、Elasticsearch、SQLiteVec、TCVector 等。

5. Telemetry
   - OpenTelemetry Trace 和 Metric；
   - OTLP HTTP/gRPC Exporter；
   - 模型和工具耗时、Token 等指标；
   - Span attribute drop、omit、truncate 策略。

## 八、当前识别出的关键缺口与风险

### 1. tenant_id 的可信来源

当前 Runtime Profile 支持从请求扩展读取 `tenant_id`。在可信内部调用中可以使用，但公开 IM Gateway 不能直接相信客户端扩展，必须通过 ChannelBinding 解析或覆盖。

### 2. 每租户选择不同数据后端

现有 Runner 通常在启动时注入一个 Session、Memory、Artifact Service。逐请求 AppName 可以隔离键空间，但不能自动让不同租户选择完全不同的后端实例。

后续可能需要：

- Tenant Service Bundle Resolver；
- 按 AppName/tenant 路由的 Service Wrapper；
- 或按租户缓存不同 Runner/Runtime。

具体选择需要进一步阅读现有 `openclaw/app/backends.go` 和 Runner 生命周期。

### 3. 消息可靠性

IM 平台通常采用至少一次投递。需要明确：

- 什么时候向平台确认成功；
- 消息什么时候算已认领；
- 节点在执行中崩溃如何恢复；
- 回复发送成功但状态保存失败怎么办；
- 如何防止危险工具重复执行。

### 4. 分布式 Session 并发

现有 Session 后端能够存储事件，并不自动代表多个 Worker 同时运行同一 Session 时完全正确。需要给出单写者、锁、分区队列或乐观并发策略。

### 5. 上游 PR 的可接受性

上游可能拒绝：

- 没有真实调用者的过度抽象；
- 大规模破坏现有接口的修改；
- 一个同时包含大量不相关模块的巨型 PR；
- 只为比赛使用、无法被普通用户复用的代码。

因此设计时应优先采用增量接口、包装器、可选能力和独立测试框架。

## 九、建议的 PR 组织方式

最终目标可以是一组上游 PR，而不是强行放入一个巨型 PR：

1. Channel Adapter 公共消息契约或一致性测试，并将现有 Telegram 纳入验证。
2. 新 IM Adapter、验签、ChannelBinding 与租户解析。
3. Inbox 幂等处理及多节点测试。
4. 多后端迁移接口、一致性测试框架和设计文档。
5. 可观测性、部署和完整示例。

如果实战平台只允许填写一个链接，可以使用主 PR，并在主 PR 描述中关联其他 PR。

在开始正式实现前，应先向上游维护者提交 Issue 或 Proposal，确认：

- 功能是否适合进入 `openclaw`；
- 公共接口应放在哪个包；
- 是否接受企业微信或其他新 Channel；
- 是否接受迁移一致性测试框架；
- PR 应如何拆分。

## 十、下一阶段学习路线

用户计划在新窗口中逐项学习前述解决内容。建议按以下顺序进行，不要立即开始完整设计：

### 阶段 1：理解现有消息链路

学习目标：能完整说明 Telegram 消息如何进入 Runner，再如何回复。

重点阅读：

- `openclaw/channel/channel.go`
- `openclaw/internal/channel/telegram/`
- `openclaw/gwproto/types.go`
- `openclaw/internal/gateway/`
- `openclaw/app/inproc_gateway_client.go`

需要回答：

- Telegram 如何接收消息？
- 何时推进 update offset？
- 如何生成 UserID、SessionID、RequestID？
- 普通回复和流式回复如何发送？
- Telegram 目前如何重试？

### 阶段 2：理解租户与 Runtime Profile

重点阅读：

- `openclaw/runtimeprofile/profile.go`
- `openclaw/runtimeprofile/selector.go`
- `openclaw/runtimeprofile/store.go`
- `openclaw/app/runtime_profile.go`
- `agent/invocation.go`
- `runner/runner.go`

需要回答：

- tenant_id 从哪里进入？
- selector 如何匹配并 fail closed？
- Profile 如何改变 AppName、模型、工具和知识库？
- AppName 如何影响 Session 和 Memory？
- 当前方案能隔离键空间，但能否切换后端？

### 阶段 3：理解 Session 与多节点一致性

重点阅读：

- `session/session.go`
- `session/redis/`
- `session/mysql/`
- `session/postgres/`
- `runner/runner.go` 中 Get/Create/AppendEvent 流程

需要回答：

- Session Key 由哪些字段组成？
- Event、State 和 Summary 以什么顺序写入？
- Redis 与 SQL AppendEvent 的原子性是什么？
- 两个 Worker 同时处理同一 Session 会发生什么？
- 是否存在唯一约束、版本号或并发保护？

### 阶段 4：学习 Webhook、验签和 IM Adapter

学习目标：能够设计一个统一但不过度抽象的 Channel Adapter。

需要学习：

- Webhook 回调和 HTTP 快速确认；
- HMAC、时间戳和 nonce；
- 企业微信或其他目标 IM 的验签方式；
- 入站消息标准化；
- 群聊、单聊、附件和卡片；
- 限流、重试和平台消息 ID。

### 阶段 5：学习幂等、Inbox/Outbox

需要理解：

- 至少一次投递；
- 幂等键和唯一约束；
- 消息认领状态机；
- Redis `SET NX`、SQL 唯一索引；
- 处理超时和租约；
- Transactional Outbox；
- 为什么“回复成功”和“状态落库”难以原子完成。

### 阶段 6：学习多后端迁移和验证

需要理解：

- 全量迁移、增量迁移、双写和切流；
- 幂等导入；
- Checkpoint 和断点续传；
- 行数、事件 ID、状态和摘要校验；
- 源后端与目标后端的规范化比较；
- 迁移回滚和 RPO/RTO；
- 如何构造跨后端一致性测试套件。

### 阶段 7：学习 OTel、审计和脱敏

重点阅读：

- `telemetry/trace/`
- `telemetry/metric/`
- `plugin/manager.go`
- `plugin/guardrail/`

需要回答：

- Trace context 如何跨 Gateway、Runner、Tool 和 IM reply 传播？
- 哪些字段适合作为 span attribute？
- 如何避免记录密钥和完整敏感消息？
- Tool visibility 和真正的 Tool authorization 有什么区别？
- 审计日志与普通运行日志有什么区别？

## 十一、正式动手前还需确认的事项

建议向导师或维护者发送如下范围说明：

> 计划使用 Go，以一组上游 PR 为主要交付；复用现有 Telegram 并新增一种 IM，完成统一消息转换、可信租户绑定和幂等处理；多后端迁移交付接口、迁移方案与跨后端一致性测试，不实现完整在线迁移系统。请问该范围是否符合实战预期？

此外还需确定：

1. 第二种 IM 具体选择。
2. 是否拥有对应平台的测试账号和公网回调条件。
3. 最终是一个 PR 还是一组关联 PR。
4. 多节点是否必须真实部署，还是集成测试和 Compose 即可。
5. 多后端迁移接口是否适合进入上游公共 API。

## 十二、新窗口可直接使用的提示词

```text
我正在完成腾讯犀牛鸟 tRPC-Agent 开源实战“多租户与节点部署”。

已确认：
1. 可以使用 Go 和 trpc-agent-go。
2. 希望最终成果是可提交上游的一组 PR。
3. 仓库已有 Telegram 只有真正纳入整体方案才算一种 IM。
4. 我计划复用 Telegram，并新增一种真实 IM。
5. 多后端迁移只做可落地设计、接口和跨后端验证测试，不实现完整生产在线迁移系统。

暂定选题：基于 tRPC-Agent-Go OpenClaw Gateway 的多租户双 IM 接入与可靠消息处理，包括可信租户绑定、消息去重、无状态 Worker、多后端迁移验证、审计和 OTel。

请先不要直接设计或写代码。请带我系统学习现有实现。本轮重点学习【填写一个主题，例如：Telegram 消息从接收到 Runner 再到回复的完整源码链路】。要求：
- 基于当前本地仓库逐文件解释；
- 说明核心类型、函数和调用关系；
- 区分仓库已有能力和选题需要新增的能力；
- 用具体消息示例解释；
- 最后给我几个检查理解的问题。
```

## 十三、当前总体结论

当前最合理的策略是：

> 不追求实现完整生产平台，而是基于仓库现有 OpenClaw、Telegram、Runtime Profile、Session/Memory 后端和 OTel，完成一条“两个 IM → 可信租户识别 → 消息幂等 → 无状态 Worker → 数据持久化 → IM 回复 → Trace/审计”的小型纵向闭环；多后端迁移以接口和验证测试为主。

判断成果质量的标准不是组件数量，而是：

- 链路是否完整；
- 租户是否真正隔离；
- 重复消息和节点故障是否有明确语义；
- 代码是否复用现有架构；
- 测试是否能够证明正确性；
- PR 是否足够小、清晰并有上游价值。
