以下可直接贴到新窗口作为上下文：

```md
## 当前目标

准备重做 “Evaluation + PromptIter 自动回归与提示词优化闭环” 的 PR 质量版本。

旧分支 `eval-optimization-loop` 已完成 phase1-phase4，但存在边界问题：phase2/phase3 修改了原始 plan 明确禁止修改的核心包 `evaluation/workflow/promptiter/engine`。因此决定不在旧分支上继续修，而是从最新 upstream main 新开干净分支重做。

## 关键判断

原始 plan 的 Assumptions 明确要求：

- 示例级交付
- 不改公共 API
- 不改核心包
- 不修改 `evaluation/workflow/promptiter`、`evaluation`、`runner`、`agent` 等核心包
- 主要实现应限制在 `examples/evaluation/promptiter_regression_loop/**`

phase2/phase3 的问题不是单纯“没用 plan 模式”，而是 scope contract 不够硬：

- phase2 为了补 `candidate.train` 引入了 `ProfileEvaluator` / `EvaluateWithProfile`，修改了 `evaluation/workflow/promptiter/engine`
- phase3 为 failure attribution 又在 `CaseResult` 透传 `ActualInvocation` / `ExpectedInvocation`
- 这些核心包改动可能有设计价值，但不能静默混入 example PR
- 如果 v2 证明确实需要核心扩展，应停下来单独做设计决策或拆成独立 PR

## 当前分支策略

采用：

- 当前目录 `D:\project\OpensourceTencent\trpc-agent-go` 冻结为旧实现参考
- 新建干净 worktree 作为 v2 开发目录
- 新分支名：`feat/eval-optimization-loop-workspace-v2`
- 新分支基于最新 `upstream/main`

用户已执行过：

```powershell
git remote -v
git fetch upstream main
git worktree add -b feat/eval-optimization-loop-workspace-v2 ..\trpc-agent-go-eval-v2 upstream/main
```

第一次创建 worktree 时遇到 Windows 路径过长：

```text
Filename too long
fatal: Could not reset index file to revision 'HEAD'
```

建议过处理方式：

```powershell
git worktree list
git worktree remove --force ..\trpc-agent-go-eval-v2
git branch -D feat/eval-optimization-loop-workspace-v2
git config --global core.longpaths true
git fetch upstream main
git worktree add -b feat/eval-optimization-loop-workspace-v2 D:\tgo-v2 upstream/main
cd D:\tgo-v2
git status -sb
```

用户已确认现在没问题。当前应假设 v2 worktree 已可用、干净，并位于较短路径，例如：

```text
D:\tgo-v2
```

## 后续开发原则

v2 必须执行严格白名单：

允许修改：

```text
examples/evaluation/promptiter_regression_loop/**
```

除非用户明确批准，否则禁止修改：

```text
evaluation/workflow/promptiter/**
evaluation/**
runner/**
agent/**
internal/**
go.mod
go.sum
```

每个阶段结束必须检查：

```powershell
git diff --name-only
```

理想结果只能出现：

```text
examples/evaluation/promptiter_regression_loop/...
```

如果出现核心包路径，必须停止并说明原因，不能继续实现。

## 旧分支作为 donor/reference

旧分支 `eval-optimization-loop` 可以作为参考实现，但不要整批 cherry-pick。

可以选择性查看/搬运：

```powershell
git show eval-optimization-loop:examples/evaluation/promptiter_regression_loop/<file>
```

或谨慎恢复白名单目录：

```powershell
git restore --source=eval-optimization-loop -- examples/evaluation/promptiter_regression_loop
git diff --name-only
```

恢复后必须确认没有带入核心包改动。

## 原始完整 plan 核心要求

目标目录：

```text
examples/evaluation/promptiter_regression_loop/
  main.go
  pipeline.go
  fake.go
  analysis.go
  report.go
  pipeline_test.go
  config/
    baseline_prompt.txt
    promptiter.json
  data/
    promptiter-regression-loop-app/
      train.evalset.json
      validation.evalset.json
      metrics.json
      trace_smoke.evalset.json
  output/
    optimization_report.json
    optimization_report.md
  README.md
  DESIGN.md
```

主链路要求：

- 使用 deterministic fake model，不依赖 API key
- 仍走真实链路：
  `llmagent -> runner -> evaluation.AgentEvaluator -> evaluation/workflow/promptiter/engine`
- fake 只替代 model 和 PromptIter worker
- 不绕过真实 patch/profile 应用路径
- patch 必须经由现有 `profilecompiler -> agent.WithSurfacePatchForNode -> llmagent` 路径影响 candidate
- 不直接 import `internal/surfacepatch`
- trace mode 不进入优化主闭环，只做 trace smoke

phase 目标：

- Phase 1：deterministic 主闭环，证明 patch 影响 candidate inference
- Phase 2：validation delta + final gate
- Phase 3：failure attribution + 过拟合拒绝
- Phase 4：trace smoke + README/DESIGN 文档收口

## v2 实现策略提醒

由于禁止修改 engine/core，v2 需要重新思考两个旧实现依赖点：

1. `candidate.train`

旧实现通过 engine 新增 `EvaluateWithProfile` 来对最终 `AcceptedProfile` 跑 train regression。v2 不允许这样做。应优先在 example 层寻找方案，例如通过现有公开 API 重新构造 evaluator / engine 输入，或者在报告中只使用可从现有 `RunResult` 安全获得的数据。如果无法做到，必须停下来报告。

2. failure attribution

旧实现通过 `CaseResult` 新增 invocation 字段获取 actual/expected invocation。v2 不允许这样做。应优先从现有 result、metric reason、trace 信息中归因。deterministic evaluator 的 failed reason 必须设计得足够稳定和可解释，用 reason/trace 完成 attribution。若确实需要 invocation 透传，必须停下来报告。

## 推荐下一步

在 v2 worktree 中先做只读检查：

```powershell
cd D:\tgo-v2
git status -sb
git log --oneline -3
git branch --show-current
```

然后读取旧 plan 和旧实现，但不要直接改：

```powershell
git show eval-optimization-loop:examples/evaluation/promptiter_regression_loop/pipeline.go
git show eval-optimization-loop:examples/evaluation/promptiter_regression_loop/report.go
git show eval-optimization-loop:examples/evaluation/promptiter_regression_loop/fake.go
```

再制定 v2 Phase 1 的最小实现计划，严格保证只改 `examples/evaluation/promptiter_regression_loop/**`。
```