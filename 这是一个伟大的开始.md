7-7:

自动回归 issue 的 数据流：

main.go
  └─ pipeline.go
       ├─ 读取 config/promptiter.json + config/baseline_prompt.txt
       ├─ 构建 fake model + llmagent + runner (fake.go)
       ├─ 构建 fake backwarder/aggregator/optimizer (fake.go)
       ├─ 创建 AgentEvaluator (读 train/validation evalset + metrics.json)
       ├─ 创建 PromptIter Engine → engine.Run()
       ├─ 结果 → analysis.go (delta / gate / attribution)
       └─ 结果 → report.go → output/*.json + output/*.md




7-8:
同步操作：
git status --short
git add -p
git status --short
git commit -m "wip: save eval optimization workspace progress"
git push origin feat/eval-optimization-loop-workspace




7-9：
同步操作，排除不想要的

# 1. 暂存所有变更
git add -A

# 2. 从暂存区移除 .cache 和 library
git reset -- .cache/ library/

# 3. 确认暂存区内容正确
git status

# 4. 提交
git commit -m "wip: save workspace progress"

# 5. 推送
git push origin feat/eval-optimization-loop-workspace




7-11:
cd D:\project\OpensourceTencent\trpc-agent-go

# 1. 确认没有遗留改动
git status

# 2. 刷新官方仓库和个人 fork 的全部远端引用
git fetch --all --prune

# 3. 更新本地 main，使其严格跟随官方 upstream/main
git switch main
git merge --ff-only upstream/main

# 4. 可选：把最新 main 同步到自己的 fork
git push origin main

# 5. 回到两主机共享的 workspace 分支
git switch feat/eval-optimization-loop-workspace

# 6. 拉取另一台主机推送的工作进度
git pull --ff-only origin feat/eval-optimization-loop-workspace

# 7. 查看官方 main 是否有新提交尚未进入当前分支
git rev-list --left-right --count HEAD...upstream/main

git status