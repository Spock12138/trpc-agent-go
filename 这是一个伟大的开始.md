7-7:

自动回归 issue 的 数据流：

main.go
  └─ pipeline.go
       ├─ 读取 config/promptiter.json + config/baseline_prompt.txt
       ├─ 构建 fake model + llmagent + runner (fake.go)
       ├─ 构建 fake backwarder/aggregator/optimizer (fake.go)
       ├─ 创建 AgentEvaluator (读 train/validation evalset + metrics.json)
       ├─ 创建 PromptIter Engine → engine.Run()
       ├─ 结果 → analysis.go (delta / gate / attribution)
       └─ 结果 → report.go → output/*.json + output/*.md