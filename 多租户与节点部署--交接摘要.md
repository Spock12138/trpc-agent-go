# 多租户与节点部署--交接摘要

> 本文件是“多租户与节点部署”开源实战的跨窗口上下文入口和当前状态唯一摘要。
>
> 最后更新：2026-08-17
>
> 当前阶段：已进入开源实战，先同步 workspace 分支并完成范围确认与现有实现学习，再开始正式编码。

## 一、每轮窗口协作协议

### 开始新窗口时

1. 首先完整读取本文件，再读取仓库适用范围内的 `AGENTS.md`。
2. 检查当前分支、工作区状态和远端跟踪状态，不把本文件中的旧提交数当作实时结果。
3. 用简短文字说明：当前目标、准备执行的本轮任务、已发现的问题或信息冲突。
4. 区分“已确认决定”“暂定方向”“待确认问题”，不得把建议或推测写成既定结论。
5. 如果本文件与代码、Git 状态、导师最新回复或用户最新指令冲突，以最新可核验证据和用户最新指令为准，并明确指出冲突。

### 本轮工作过程中

- 先理解现有设计，再做最小、可测试、适合上游审查的改动。
- 不擅自扩大选题范围，不为尚无真实调用者的场景新增大量公共抽象。
- 重要结论必须有源码、测试、Git 状态、官方资料或导师回复作为依据。
- 涉及公共 API 或外部可观察行为时，遵守 `AGENTS.md` 的二次设计审查要求。

### 结束窗口前

1. 更新本文件中的“当前阶段”“已完成”“当前问题”“下一步”和“Git 状态”。
2. 把本轮新增的可靠结论归入相应章节；过时结论应直接修正，并记录修正原因。
3. 在“最近窗口记录”中新增一条简短记录，只保留最近 5 轮，较早内容归并到正文。
4. 最终回复必须概括：本轮完成内容、验证结果、未解决问题、下一窗口第一步。

## 二、课题与交付目标

- 课题：腾讯犀牛鸟 tRPC-Agent 开源实战“多租户与节点部署”。
- 实现语言和仓库：Go，`trpc-group/trpc-agent-go`。
- 主要交付目标：形成可提交上游、可测试、可审查的一组小型 PR，而不只是外部设计文档。
- 总体策略：收缩实现宽度，但保持完整业务闭环；不追求一次实现完整生产 SaaS 平台。

题目要求覆盖以下能力：

1. 租户模型、配置与数据隔离、工具权限、密钥管理和日志脱敏。
2. Gateway、Worker、Channel Adapter、Storage Adapter、Admin API、Telemetry Collector 等组件协作。
3. 多节点水平扩展、正确的租户与 Session 路由、共享后端和无状态 Worker。
4. Session、Memory、Summary、Artifact、Knowledge、Audit Log 等数据后端与迁移。
5. 至少两种真实 IM 接入，以及验签、租户绑定、身份映射、消息去重和回复。
6. 治理、监控、OpenTelemetry、审计、故障恢复、灰度、回滚、容量和部署方案。

## 三、当前方案定位

暂定定位：

> 基于 tRPC-Agent-Go OpenClaw Gateway 的多租户双 IM 接入与可靠消息处理：复用 Telegram，新增一种真实 IM，实现可信租户绑定与消息幂等，并提供多后端迁移接口和一致性验证方案。

目标纵向链路：

```text
IM callback
  -> Channel Adapter 验签和消息标准化
  -> 由可信 ChannelBinding 解析 tenant_id
  -> 以平台 message_id 做幂等认领
  -> 生成 user_id 和 session_id
  -> Runtime Profile 选择 Agent、模型、工具和隔离配置
  -> 无状态 Worker 执行 Runner
  -> 共享后端保存 Session / Memory / Summary / Audit
  -> Agent Event 转换为 IM 回复
  -> Trace、Metric 和审计贯穿全链路
```

多后端迁移暂定只实现可落地接口、导出/导入/校验流程和跨后端一致性测试；完整在线双写、灰度切流和回滚以设计说明为主。

## 四、已经确认的决定

1. 导师已确认题目可以使用 Go 和 `trpc-agent-go`。
2. 仓库已有 Telegram 只有真正进入整体代码与测试链路，才能计为两种 IM 中的一种。
3. 新增一种真实 IM，且与 Telegram 共用可信租户识别、Session、Runner 和回复链路。
4. 最终成果优先组织成一组范围清晰的小型上游 PR。
5. 多节点倾向使用共享后端和无状态 Worker；不依赖 sticky session，但同一 Session 仍需顺序或并发控制。
6. workspace 分支用于两台主机同步代码、草稿和笔记，不直接向上游提交 PR。
7. 最终 PR 分支从最新 `upstream/main` 创建，只迁移正式代码、测试和正式文档。

## 五、待确认事项

以下事项尚无最终答案，正式实现前应向导师或上游维护者确认，或由用户明确决定：

1. 第二种 IM 具体选择；有企业微信测试账号和公网回调条件时优先企业微信，否则考虑飞书或 Slack。
2. 是否具备目标 IM 的测试账号、域名或公网 webhook 条件。
3. 实战最终提交一个主 PR，还是一组关联 PR。
4. 多节点要求是否可由集成测试和最小 Docker Compose 验证，还是必须真实部署。
5. 多后端迁移能力是否适合进入上游公共 API，以及应归属哪个包。
6. 上游是否接受新增 IM、ChannelBinding、Inbox 幂等能力和建议的 PR 拆分方式。

建议在正式编码前提交一份简短 Proposal，说明：复用 Telegram、新增一种 IM、实现可信租户绑定与幂等，多后端迁移只交付接口和跨后端验证，不实现完整在线迁移平台。

## 六、仓库已有基础

- `openclaw/runtimeprofile`：按 tenant/channel/user/session 选择 Profile，fail closed，并生成逐请求 `RunOptions`。
- `agent` 与 `runner`：逐请求 AppName、动态 Agent、工具过滤和授权、Plugin/Callback/Guardrail、事件流和 Session 持久化。
- `openclaw`：HTTP Gateway、统一 `MessageRequest` / `StreamEvent`、Telegram Channel、Channel/Backend Factory Registry、默认群聊和单聊 Session ID 规则。
- Session 后端：InMemory、Redis、SQLite、MySQL、PostgreSQL、MongoDB、ClickHouse、pgvector 等。
- Memory、Artifact、Knowledge：已有多种本地、数据库、对象存储、向量库和外部服务实现。
- Telemetry：OpenTelemetry Trace/Metric、OTLP Exporter、模型/工具耗时与 Token 指标、属性丢弃/省略/截断策略。

## 七、关键缺口与风险

1. 外部请求中的 `tenant_id` 不可信，必须由验签后的 IM 账号或机器人绑定关系在服务端解析。
2. Runtime Profile 能隔离 AppName 和键空间，但现有 Runner 启动时注入后端，尚不能自然地按租户切换完全不同的后端实例。
3. IM 至少一次投递需要 Inbox 幂等认领、唯一约束、处理状态和故障恢复语义；危险工具尤其不能重复执行。
4. 共享 Session 后端不等于多 Worker 并发安全；需要单写者、分区队列、分布式锁或版本/CAS 策略。
5. “回复发送成功”和“处理状态落库”难以原子完成；可能需要明确 Outbox 或补偿语义。
6. 上游可能拒绝无真实调用者的过度抽象、破坏兼容性的公共 API、巨型 PR 或仅为比赛服务的代码。

## 八、建议学习与实现顺序

1. Telegram 从消息接收、update offset、ID 生成、Gateway/Runner 到普通和流式回复的完整源码链路。
2. Runtime Profile 的 tenant 来源、selector、fail closed、AppName 隔离和后端生命周期。
3. Session 的 Get/Create/AppendEvent、Event/State/Summary 顺序，以及 Redis/SQL 并发语义。
4. 目标 IM 的 Webhook、验签、快速确认、消息标准化、平台限制和重试。
5. Inbox/Outbox、幂等键、认领状态机、租约、唯一索引和故障恢复。
6. 多后端导出、导入、Checkpoint、幂等重试和一致性验证测试。
7. OTel、审计字段、敏感信息脱敏和工具授权。

建议的 PR 顺序：公共消息契约/Telegram 验证 -> 新 IM 与可信租户绑定 -> Inbox 幂等与多节点测试 -> 多后端迁移验证 -> 观测、部署和完整示例。

## 九、Git 与两主机工作区约定

工作目录：

```text
D:\project\OpensourceTencent\trpc-agent-go-multi-tenant-workspace
```

分支：

```text
workspace: feat/multi-tenant-node-deployment-workspace
final PR: feat/multi-tenant-node-deployment
```

远端实际状态（2026-08-17 本地检查）：

```text
origin   -> https://github.com/Spock12138/trpc-agent-go.git
spock    -> https://github.com/Spock12138/trpc-agent-go.git
upstream -> https://github.com/trpc-group/trpc-agent-go.git
```

旧的两主机摘要写着“没有 `spock` remote”，该信息已经过时。`origin` 与 `spock` 当前指向同一 fork；后续命令统一使用 `origin`，无需为此删除 `spock`。

当前 workspace 分支：

```text
HEAD: e2b7185c docs: update multi-tenant workspace sync notes
tracking: origin/feat/multi-tenant-node-deployment-workspace
```

本地 `upstream/main` 仍停在 2026-07-20 的 `0c777418`，尚未 fetch 最新官方主线。本地旧引用因此显示 ahead 1、behind 0；GitHub 截图当时显示 ahead 1、behind 101。真实差异必须在 `git fetch upstream --prune` 后重新计算。

workspace 分支已推送且由两台主机共享。同步官方主线时采用 merge，不用 rebase/force-push 改写共享历史。最终 PR 分支仍直接基于最新 `upstream/main`，所以 workspace 中的同步 merge 不会污染最终 PR。

### 建议的 workspace 上游同步流程

当前工作区存在未跟踪文档，先用 stash 临时保护；不要直接在脏工作区 merge：

```powershell
cd D:\project\OpensourceTencent\trpc-agent-go-multi-tenant-workspace

git switch feat/multi-tenant-node-deployment-workspace
git status --short --branch
git stash push -u -m "before syncing upstream main"

git fetch origin --prune
git pull --ff-only origin feat/multi-tenant-node-deployment-workspace
git fetch upstream --prune

git rev-list --left-right --count HEAD...upstream/main
git log --oneline --decorate --graph --max-count=20 HEAD upstream/main

git merge --no-edit upstream/main
git status
```

若 merge 有冲突，先查看 `git status`，逐个解决并测试，然后 `git add <已解决文件>`、`git commit`；不要强推。若不准备继续解决，可在尚未提交 merge 时使用 `git merge --abort` 回到合并前。

merge 成功后恢复本轮文档并提交：

```powershell
git stash pop
git status

git add -- "多租户与节点部署--交接摘要.md" `
  "开源实战题目.md" `
  "开源实战选题上下文摘要.md"
git commit -m "docs: add multi-tenant deployment handoff"

go test ./...
git status
git log --oneline --decorate --graph --max-count=20
git push origin feat/multi-tenant-node-deployment-workspace
git status
```

`task_plan.md`、`findings.md`、`progress.md` 是本轮过程记录，不是正式交接入口；其中 `progress.md` 会被仓库现有的 `pr*.md` 规则忽略，默认不纳入两主机同步。不要在未确认时用 `git add -A` 把所有未跟踪文件一起提交。若 `git stash pop` 发生冲突，stash 通常会保留，先解决冲突并确认内容完整，再考虑删除对应 stash。

## 十、当前状态与下一步

### 本轮已完成

- 创建本交接摘要并定义跨窗口读写协议。
- 汇总题目、已确认决定、暂定方案、待确认问题、已有能力、风险和建议路线。
- 核验当前分支、tracking 和远端；发现旧摘要中的 `spock` 描述已过时。
- 确认本地 `upstream/main` 引用过旧，尚未执行 fetch、merge、rebase 或 push。

### 当前问题

- 工作区存在未跟踪的前情文档和本轮记录，执行上游同步前必须先明确提交或暂存，保持工作区干净。
- 官方主线实时 ahead/behind 数量尚未在本地核验；截图中的 behind 101 只能作为当时状态。
- 第五节列出的范围问题仍未最终确认。

### 下一窗口第一步

1. 完整读取本文件和 `AGENTS.md`。
2. 运行只读 Git 检查，确认没有另一台主机的新提交或本地未处理改动。
3. 先保存当前文档，再 fetch `origin` 和 `upstream`，重新计算 ahead/behind。
4. 将最新 `upstream/main` merge 到 workspace 分支，验证后 push 到 `origin`。
5. Git 同步完成后，若导师范围仍未确认，先准备 Proposal；同时从 Telegram 完整消息链路开始源码学习。

## 十一、最近窗口记录

### 2026-08-17：建立交接机制并准备同步上游

- 新建本交接摘要，约定每轮开始先读取、结束前更新。
- 将三份前情材料整合为可靠的当前上下文。
- 发现实际存在 `spock` remote，修正旧摘要信息。
- 发现本地 `upstream/main` 引用过旧；没有执行任何 Git 同步写操作。
- 下一步是保存文档、fetch 后核验差异，并以 merge 更新共享 workspace 分支。
